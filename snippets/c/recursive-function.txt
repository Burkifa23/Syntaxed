#include <stdio.h>

int factorial(int n); 

int main() {

    int n = 5;
    printf("%d! = %d\n", n, factorial(n));
    
    return 0;

}

int factorial(int n) {
    // Base case
    if (n <= 1) {
        return 1;
    }
    // Recursive case
    return n * factorial(n - 1);
}