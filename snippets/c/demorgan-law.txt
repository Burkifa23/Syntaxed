#include <stdio.h>

int main(){
    int a = 1, b = 0;
    printf("a = %d, b = %d\n", a, b);
    printf("!(a && b) = %d\n", !(a && b));
    printf("(!a || !b) = %d\n", (!a || !b));
    printf("These should be equal (De Morgan's Law)\n");
    
    return 0;

}