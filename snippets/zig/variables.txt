// Variables in Zig

// Declare a mutable variable
var age: u32 = 30;
comptime var pi: f32 = 3.14159; // Compile-time constant

// Inferring type
const name = "Alice";

// Pointers
var x: i32 = 10;
const ptr_x: *i32 = &x;

// Optionals (for values that might be null)
const maybe_value: ?u8 = null;